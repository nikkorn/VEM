package gaia.world.items.container;

/**
 * Emuneration of container update behaviours.
 * Defines how a container should behave when changing from one type to another.
 */
public enum ContainerUpdateBehaviour {
	EMPTY,
	TRANSFER
}
